{
  "name": "source-review-benchmark-tools",
  "version": "0.1.0",
  "private": false,
  "description": "Corpus tooling for the HapDo source-review benchmark: inventory, splicer, marker stripper, line map, leak check.",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "vitest run",
    "leak-check": "node bin/leak-check.mjs"
  },
  "dependencies": { "diff": "^9.0.0" },
  "devDependencies": { "vitest": "^3.2.4" }
}
